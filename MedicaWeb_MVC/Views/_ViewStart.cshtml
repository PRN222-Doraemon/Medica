@using Core.Entities.Identity

@{
	if (User.IsInRole(UserRoles.Admin))
	{
		Layout = "_LayoutAdmin";
	} else if (User.IsInRole(UserRoles.Lecturer))
	{
		Layout = "_LayoutLecturer";
	} else
	{
		Layout = "_LayoutLecturer";
	}
}
