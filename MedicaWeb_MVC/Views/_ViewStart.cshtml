@{
	if (User.IsInRole(AppCts.Roles.Admin))
	{
		Layout = "_LayoutAdmin";
	}
	else if (User.IsInRole(AppCts.Roles.Lecturer) || User.IsInRole(AppCts.Roles.Employee))
	{
		Layout = "_LayoutLecturer";
	}
	else
	{
		Layout = "_Layout";
	}
}
