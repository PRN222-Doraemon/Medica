@model CourseVM

<div class="col-lg-4">
    <div class="card shadow-sm position-sticky" style="top: 2rem;">
        <img src="@Model.ImgUrl" class="card-img-top" alt="@Model.Name">
        <div class="card-body">
            <span class=" fw-bold text-dark fs-1">
                ₫ @($"{Model.Price:N0}")
            </span>
            <a asp-action="Index" asp-controller="Classrooms" asp-route-courseId="@Model.Id" class="btn btn-primary btn-lg w-100 mb-4 mt-3">View Classes</a>

            <h5 class="mb-3">This course includes:</h5>
            <ul class="list-unstyled mb-0">
                <li class="d-flex align-items-center gap-2 mb-3">
                    <i class="bi bi-camera-video text-primary"></i>
                    <span>@Model.Duration on demand video</span>
                </li>
                <li class="d-flex align-items-center gap-2 mb-3">
                    <i class="bi bi-download text-primary"></i>
                    <span>Downloadable resources</span>
                </li>
                <li class="d-flex align-items-center gap-2 mb-3">
                    <i class="bi bi-phone text-primary"></i>
                    <span>Access on mobile and TV</span>
                </li>
                <li class="d-flex align-items-center gap-2">
                    <i class="bi bi-award text-primary"></i>
                    <span>Certificate of completion</span>
                </li>
            </ul>
        </div>
    </div>
</div>